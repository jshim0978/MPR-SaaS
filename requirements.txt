# PRaaS/MPR-SaaS - Prompt Refinement as a Service
# Core dependencies for deployment

# Core ML frameworks
transformers>=4.38.0
torch>=2.1.0
peft>=0.8.0

# Web services
fastapi>=0.109.0
uvicorn>=0.27.0
requests>=2.31.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0

# Monitoring (optional)
prometheus-client>=0.19.0

# Training (optional, only needed on training server)
# datasets>=2.16.0
# accelerate>=0.26.0
# wandb>=0.16.0

